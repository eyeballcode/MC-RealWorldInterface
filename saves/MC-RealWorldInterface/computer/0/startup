rednet.open('top')

while true do
  sender, message = rednet.receive('MC-RealWorldInterface-ServerRoomLock')
  if message == 'toggle' then
    redstone.setOutput('left', true)
    sleep(.1)
    redstone.setOutput('left', false)
  end
end
