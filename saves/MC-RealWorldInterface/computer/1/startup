rednet.open('top')

while true do
  senderID, message = rednet.receive('MC-RealWorldInterface-ServerRoomLock')
  print(message)
  if message == 'toggle' then
    redstone.setOutput('back', true)
    sleep(0.1)
    redstone.setOutput('back', false)
  end
end
