rednet.open('top')

while true do
  sender, message = rednet.receive('MC-RealWorldInterface-LightsControl')
  if message == 'toggle' then
    redstone.setOutput('front', true)
    sleep(.5)
    redstone.setOutput('front', false)
  end
end
