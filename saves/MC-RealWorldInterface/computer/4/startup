rednet.open('right')

while true do
  sender, message = rednet.receive('MC-RealWorldInterface-TempRoomDoor')
  if message == 'toggle' then
    redstone.setOutput('top', true)
    sleep(.1)
    redstone.setOutput('top', false)
  end
end
